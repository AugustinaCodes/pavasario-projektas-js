services:
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: fitbook_api
    ports:
      - "3000:3000"
    volumes:
      - ./backend:/app
      - /app/node_modules
    env_file:
      - ./backend/.env
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: fitbook
      DB_USER: postgres
      DB_PASSWORD: postgres
      CHOKIDAR_USEPOLLING: "true"
      CHOKIDAR_INTERVAL: "300"
    depends_on:
      - db
    restart: unless-stopped

  db:
    image: postgres:16
    container_name: fitbook_db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: fitbook
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - fitbook_postgres_data:/var/lib/postgresql/data
      - ./backend/database/init.sql:/docker-entrypoint-initdb.d/01-init.sql
      - ./backend/database/seed.sql:/docker-entrypoint-initdb.d/02-seed.sql
    restart: unless-stopped

  pgadmin:
    image: dpage/pgadmin4
    container_name: fitbook_pgadmin
    ports:
      - "5050:80"
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@fitbook.com
      PGADMIN_DEFAULT_PASSWORD: admin123
    depends_on:
      - db
    restart: unless-stopped

volumes:
  fitbook_postgres_data: